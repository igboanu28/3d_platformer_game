using Platformer;

namespace CombatSystem
{
    public class AttackDataEventListener : EventListener<AttackSO>
    {
        // This class simply makes EventListener<AttackData> concrete
        // The functionality is in the base EventListener<T> and the UnityEvent in Inspector
    }
}